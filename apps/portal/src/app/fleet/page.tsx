import { PortalPlaceholder } from '../../components/PortalPlaceholder';

export default function FleetPage() {
  return (
    <PortalPlaceholder
      title="Mobile fleet & SIMs"
      description="Manage SIM cards, mobile numbers, and fleet assignments for your organisation."
    />
  );
}
