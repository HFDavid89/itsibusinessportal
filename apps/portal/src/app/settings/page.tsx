import { PortalPlaceholder } from '../../components/PortalPlaceholder';

export default function SettingsPage() {
  return (
    <PortalPlaceholder
      title="Settings"
      description="Configure notification preferences, security options, and portal access for your account."
    />
  );
}
