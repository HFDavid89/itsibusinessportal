import { PortalPlaceholder } from '../../components/PortalPlaceholder';

export default function TicketsPage() {
  return (
    <PortalPlaceholder
      title="Support tickets"
      description="Raise new support requests and track the status of open tickets."
    />
  );
}
