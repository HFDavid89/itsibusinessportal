import { PortalPlaceholder } from '../../components/PortalPlaceholder';

export default function ServicesPage() {
  return (
    <PortalPlaceholder
      title="Active services"
      description="View your broadband, mobile, energy, and other active business services."
    />
  );
}
